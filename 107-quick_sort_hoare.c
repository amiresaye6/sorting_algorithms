#include "sort.h"

/**
 * quick_sort_hoare - sorts an array using the quick_sort_hoare sort algorithm
 * @array: the array to be sorted.
 * @size: the size of the array.
 *
 * Return: Void
 */
void quick_sort_hoare(int *array, size_t size)
{
	if (array == NULL || size < 2)
		return;
}
