#include "sort.h"

/**
 * counting_sort - sorts an array using the Quick sort algorithm
 * @array: the array to be sorted.
 * @size: the size of the array.
 *
 * Return: Void
 */
void counting_sort(int *array, size_t size)
{
	if (array == NULL || size < 2)
		return;
}
